# animevost.org
```
branches:
    # not ready yet
    @master  -> bash script      | download series with linux commands
    + ./animevost.sh [url]       | commands: wget, curl, grep

    @master, (pyscr3 in t.f)     | download series with python3 modules
    + python3 animevost.py [url] | modules:  os, sys, bs4, glob, json, wget, requests, multiprocessing
```
